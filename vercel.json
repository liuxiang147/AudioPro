{
  "buildCommand": "pip install -r requirements.txt && python3 manage.py collectstatic --noinput --clear --no-post-process && python3 manage.py check --deploy --fail-level WARNING",
  "outputDirectory": "staticfiles",
  "devCommand": "python3 manage.py runserver",
  "installCommand": "pip install -r requirements.txt",
  "framework": "django"
}
